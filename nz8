#include<iostream>

using namespace std;

 int main ()
 {
     int N;
     cin >> N;
     if(N % 5 > 0)
         
     {
         cout << "-1\n";
         continue;
     }
     if(N ==-1)
     {
         break;
     }
     cout << n\5 << "\n";
 }
 cout << "Goodbye!\n";
     
     return 0;
 }
     
     
